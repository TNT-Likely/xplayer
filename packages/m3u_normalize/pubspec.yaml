name: m3u_normalize
description: >-
  把 M3U/IPTV 播放列表里的机器标识清洗成人类可读的频道名，
  并把分号粘连的 group-title 拆成多标签。纯 Dart，无 Flutter 依赖。
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
